#include<iostream>
using namespace std;

int main() {

		    int a,b,c,d,n;
			cin>>n;
		    a=n;
		    while(n>0)
		    {
		        b=n%10;
		        n=n/10;
		        d=(d*10)+b;
		    }
		    if(d==a) cout<<"true";
		    else cout<<"false";
	
}
