#include <iostream>
using namespace std;

int main() {
	int a[2],b,i;
	cin>>b;
	for(i=0;i<b;i++)
{
    cin>>a[0]>>a[1];
    if((a[0]*100)>(a[1]*10) || (a[0]*100)==(a[1]*10))
    {
        cout<<"Cloth\n";
    }
    else
    {
        cout<<"Disposable\n";
    }
}
	return 0;
}
