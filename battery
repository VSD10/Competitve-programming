//if charge is less than 15% it shows notification otherwise no
#include <iostream>
using namespace std;

int main() {
	int a,b,i;
	cin>>a;
	for(i=0;i<a;i++)
	{
	    cin>>b;
	 
	    if(b>15)
	    {
	       cout<<"No"<<endl;
	    }
	    else
	    {
	        cout<<"Yes"<<endl;
	    }
	}
	return 0;
}
